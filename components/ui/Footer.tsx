import React from 'react';
import { leckerli } from '@/app/fonts';

function Footer() {
  return (
    <footer>
        <div>
            <h1 className={`${leckerli.className} text-lg text-white`}>Kirai</h1>
        </div>
    </footer>
  )
}

export default Footer