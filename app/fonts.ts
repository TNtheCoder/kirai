import { Inter, Roboto_Mono, Roboto, Leckerli_One } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const roboto_mono = Roboto_Mono({
  subsets: ['latin'],
  display: 'swap',
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
});

export const leckerli = Leckerli_One({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
});
